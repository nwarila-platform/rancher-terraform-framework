# Terraform Module Reference

Auto-generated by `terraform-docs` from the HCL in [`terraform/`](../../terraform/).

<!-- BEGIN_TF_DOCS -->


## Module Documentation

Auto-generated by `terraform-docs` from the HCL in [`terraform/`](../../terraform/).
Run `make docs` to regenerate; CI fails if this file is out of sync via
`make docs-diff`.

## Requirements

| Name | Version |
| ---- | ------- |
| <a name="requirement_terraform"></a> [terraform](#requirement\_terraform) | = 1.15.4 |
| <a name="requirement_local"></a> [local](#requirement\_local) | = 2.8.0 |
| <a name="requirement_null"></a> [null](#requirement\_null) | = 3.2.4 |
| <a name="requirement_random"></a> [random](#requirement\_random) | = 3.8.1 |
| <a name="requirement_time"></a> [time](#requirement\_time) | = 0.13.1 |
| <a name="requirement_tls"></a> [tls](#requirement\_tls) | = 4.2.1 |

## Providers

| Name | Version |
| ---- | ------- |
| <a name="provider_local"></a> [local](#provider\_local) | = 2.8.0 |
| <a name="provider_null"></a> [null](#provider\_null) | = 3.2.4 |
| <a name="provider_random"></a> [random](#provider\_random) | = 3.8.1 |
| <a name="provider_time"></a> [time](#provider\_time) | = 0.13.1 |
| <a name="provider_tls"></a> [tls](#provider\_tls) | = 4.2.1 |

## Resources

| Name | Type |
| ---- | ---- |
| [local_file.manifest](https://registry.terraform.io/providers/hashicorp/local/2.8.0/docs/resources/file) | resource |
| [null_resource.lifecycle_hook](https://registry.terraform.io/providers/hashicorp/null/3.2.4/docs/resources/resource) | resource |
| [random_pet.environment](https://registry.terraform.io/providers/hashicorp/random/3.8.1/docs/resources/pet) | resource |
| [random_string.environment_secret](https://registry.terraform.io/providers/hashicorp/random/3.8.1/docs/resources/string) | resource |
| [time_rotating.environment_rotation](https://registry.terraform.io/providers/hashicorp/time/0.13.1/docs/resources/rotating) | resource |
| [time_static.environment_created](https://registry.terraform.io/providers/hashicorp/time/0.13.1/docs/resources/static) | resource |
| [tls_private_key.environment](https://registry.terraform.io/providers/hashicorp/tls/4.2.1/docs/resources/private_key) | resource |
| [tls_self_signed_cert.environment](https://registry.terraform.io/providers/hashicorp/tls/4.2.1/docs/resources/self_signed_cert) | resource |
| [local_file.runner_inventory](https://registry.terraform.io/providers/hashicorp/local/2.8.0/docs/data-sources/file) | data source |
| [local_file.tier_defaults](https://registry.terraform.io/providers/hashicorp/local/2.8.0/docs/data-sources/file) | data source |

## Inputs

| Name | Description | Type | Default | Required |
| ---- | ----------- | ---- | ------- | :------: |
| <a name="input_all_environments"></a> [all\_environments](#input\_all\_environments) | Every synthetic environment this framework should produce. Each entry expands (in locals.tf) to a bundle of synthetic resources demonstrating the framework pattern: required + optional scalars, list-of-objects with iterative dynamic blocks, single-optional with splat-on-optional dynamic blocks, and tier-based defaults injected via the data-source-injection pattern. | <pre>list(<br/>    object({<br/><br/>      /* Required Parameters */<br/>      name  = string<br/>      owner = string<br/>      tier  = string<br/><br/>      /* Optional Parameters */<br/>      enabled        = optional(bool, true)<br/>      retention_days = optional(number)<br/>      # ^ Default deliberately ABSENT here. Defaulting happens in locals.tf<br/>      # using data.local_file.tier_defaults so per-tier defaults are<br/>      # applied. Demonstrates "computed default from data source" pattern.<br/>      description = optional(string)<br/>      tags        = optional(list(string), [])<br/><br/>      manifests = optional(<br/>        list(<br/>          object({<br/><br/>            /* Required Parameters */<br/>            filename = string<br/>            content  = string<br/><br/>            /* Optional Parameters */<br/>            permissions      = optional(string, "0644")<br/>            directory_create = optional(bool, true)<br/>          })<br/>        )<br/>      )<br/>      lifecycle_hooks = optional(<br/>        list(<br/>          object({<br/><br/>            /* Required Parameters */<br/>            name = string<br/><br/>            /* Optional Parameters */<br/>            triggers = optional(map(string), {})<br/>            # description: use triggers["description"] instead; null_resource has no native description.<br/>          })<br/>        )<br/>      )<br/>      # rotation: single optional block exercising the splat-on-optional dynamic pattern.<br/>      rotation = optional(<br/>        object({<br/>          rotation_days  = optional(number)<br/>          rotation_hours = optional(number)<br/>          # rotation_minutes: omitted intentionally; sub-hour rotation is non-idiomatic for this showcase.<br/>          # triggers: pulled from environment.tags + global_tag at locals expansion.<br/>        })<br/>      )<br/>      pet = optional(<br/>        object({<br/>          length = optional(number)<br/>          # ^ Default deliberately ABSENT — comes from tier_defaults.default_pet_length<br/>          separator = optional(string, "-")<br/>          prefix    = optional(string)<br/>          # length_override: see `pet.length`; explicit override wins over tier default.<br/>        })<br/>      )<br/>      # certificate: single optional block with a nested optional subject block.<br/>      certificate = optional(<br/>        object({<br/><br/>          /* Optional Parameters */<br/>          validity_period_hours = optional(number, 720) # 30 days<br/>          early_renewal_hours   = optional(number)<br/>          is_ca_certificate     = optional(bool, false)<br/>          set_subject_key_id    = optional(bool, false)<br/>          allowed_uses = optional(list(string), [<br/>            "key_encipherment",<br/>            "digital_signature",<br/>            "server_auth",<br/>          ])<br/>          # private_key_pem: generated by tls_private_key in main.tf; never accepted from input.<br/><br/>          subject = optional(<br/>            object({<br/>              common_name         = optional(string)<br/>              country             = optional(string)<br/>              locality            = optional(string)<br/>              organization        = optional(string)<br/>              organizational_unit = optional(string)<br/>              postal_code         = optional(string)<br/>              province            = optional(string)<br/>              serial_number       = optional(string)<br/>              street_address      = optional(list(string))<br/>            })<br/>          )<br/>        })<br/>      )<br/>    })<br/>  )</pre> | n/a | yes |
| <a name="input_environment_prefix"></a> [environment\_prefix](#input\_environment\_prefix) | Prefix prepended to every generated artifact (file names, Terraform resource addresses, output identifiers). Lets multiple deployments of this framework coexist in one workspace without collision. | `string` | `"demo"` | no |
| <a name="input_global_tag"></a> [global\_tag](#input\_global\_tag) | Free-form label applied to every synthetic resource as a `triggers` map entry or tag attribute. Used by tests to assert resources came from the expected apply. | `string` | `"framework-example"` | no |
| <a name="input_secret_seed"></a> [secret\_seed](#input\_secret\_seed) | Optional sensitive salt hashed into every generated random\_string keeper. Changing it rotates generated random strings without storing the raw value in state. Inject via terraform.tfvars or a TF\_VAR\_secret\_seed environment variable; do not commit real secrets. | `string` | `null` | no |

## Outputs

| Name | Description |
| ---- | ----------- |
| <a name="output_environment_certificates"></a> [environment\_certificates](#output\_environment\_certificates) | Per-environment generated key + cert pairs (PEM-encoded). Sensitive due to private\_key\_pem. Only populated for environments where certificate != null in input. |
| <a name="output_environment_secrets"></a> [environment\_secrets](#output\_environment\_secrets) | Per-environment synthetic secrets. Sensitive — Terraform redacts these in CLI output and any reference to them in downstream config inherits the sensitivity flag automatically. |
| <a name="output_environments"></a> [environments](#output\_environments) | Map keyed by prefixed environment resource key (`<environment_prefix>-<name>`). Each entry composes references from every per-env synthetic resource: the generated pet name, the creation timestamp, and pointers to the resources produced for that environment. |
| <a name="output_framework_summary"></a> [framework\_summary](#output\_framework\_summary) | Aggregate counts of every resource type produced by this framework. Stable across applies for a given input — useful for tests asserting on resource creation. |
| <a name="output_manifest_paths"></a> [manifest\_paths](#output\_manifest\_paths) | Absolute path of every local\_file produced by this framework, keyed by composite\_key (`<environment_prefix>-<name>__filename`). Useful for downstream consumers (e.g. a runner overlay validator) to enumerate generated artifacts. |
| <a name="output_runner_inventory"></a> [runner\_inventory](#output\_runner\_inventory) | Runner-owned files discovered under terraform/repos/, keyed by repository-relative path. This proves overlays are consumed by the framework rather than merely copied beside it. |
<!-- END_TF_DOCS -->

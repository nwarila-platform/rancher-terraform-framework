apiVersion: v1
kind: Service
metadata:
  name: compliant-fixture
  labels:
    app.kubernetes.io/name: compliant-fixture
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: compliant-fixture
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
  ports:
    - name: http
      port: {{ int .Values.service.port }}
      targetPort: http
      protocol: TCP
